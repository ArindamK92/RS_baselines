### Compute SHII

## Run:  
------------  
Generate shs_multiMethod_<graphFilename>.txt by running baselines from ../baseline folders and keep in SHII folder for computation.  
Keep community_<graphFilename>.txt also in the same folder.  

```
python compute_shii.py -f graphFilename.mtx
```